---
name: flatland-research-loop
license: CC-BY-NC-4.0
description: "Optimize an existing Flatland solver toward full marks by default. Inspect its implementation, obtain current scores and scoring rules, choose substantial algorithmic improvements or targeted trials, and iterate with actual quality feedback. Agent-agnostic; optional learning. 不提供原作者解法或满分配置。"
---

# Flatland Research Loop · 自动优化

**简体中文** | [English](SKILL.en.md)

接手使用者的 Flatland 项目，**默认向当前 benchmark 的满分 / 100% 优化**，无需再问目标是不是满分。用户明确指定其他目标、只修一个错误或学习时，按其范围工作。满分是工作目标，是否达到由实际评分确认；它不等于所有列车到达，也不代表对所有 Flatland 环境最优。

## 第一件事：检查当前方案，拿到成绩与评分规则

先检查当前求解器与评测入口：状态和时间怎么表示、怎样避冲突、初始规划与重规划用了什么、是否有联合改进、主要计算花在哪里。以代码和运行记录判断能力，不能只看算法名称。按 [接手与诊断指南](references/diagnosis.md) 核对：

- **当前方案和最好版本**：实际运行的版本、已有算法组合、已知失败场景，以及当前版本是否就是最高分版本。
- **当前得分与具体评分**：最新分数、历史最好分数、对应版本及逐项 / 逐例结果；总分分母、权重、惩罚、聚合方式、benchmark 与运行限制。
- **可运行的评测条件**：本地与服务器入口、现有结果和资源边界。区分官方成绩、官方本地评估与自定义代理。

先读用户已给的代码、成绩和规则，**缺什么就直接问什么**，不要让用户重复提交。缺少成绩与规则时，合并为一句，例如：“请提供当前和最好版本的得分及对应代码版本、详细评分结果和评分规则（现有报告或截图即可）。” 如果用户尚未跑过，记录“未测”，使用可用原生入口建立基线；规则不明就保持满分目标待定义，不自造百分比。等待回答期间继续代码检查和可独立完成的准备，依赖评分口径的取舍待信息明确后进行。

给出简短接手判断：**现在是什么水平 → 主要失分或能力缺口 → 默认满分还差什么 / 哪些尚未知 → 应做架构升级还是针对性强化。**

## 根据起点选择改动幅度

如果当前只靠独立最短路、单一优先级或缺乏时间与冲突处理，而代码和成绩表明这些限制正造成大量失分，直接评估并实施更有能力的规划、联合搜索或执行协调方案。可以将必要且紧密关联的改动作为一个候选；无需先把每个简单组合都调到极限。已成熟的系统则围绕实际剩余损失强化，不因算法名字朴素就重写。

按需读 [算法机制指南](references/algorithm-playbook.md) 与 [真实决策案例](references/reasoning-cases.md)。将有方向的搜索和 SIPP 的可行性表达、PP 的资源分配、LNS 的联合改变、路线与时序多样性、依赖协调和故障修复、增量维护与公平覆盖，转成**当前项目缺少的能力**。算法组合、参数、实例规模与预算分配都可以是改进对象；这些思想用于选择方案，不是要求照装一条固定流水线。

## 实现、实测、保留、继续

1. **形成可执行候选。** 说明改变哪个决策、为何值得试、看什么结果决定去留。允许有界的经验性参数尝试、组合比较和 benchmark 适配；合理猜想可以先试再解释，不要求每轮先有完整因果证明。
2. **短冒烟后测实际质量。** 正确性检查通过后，默认让候选求解器真实参与代表性实例，测主指标、退步、失败与耗时。按 [质量评估](references/quality-evaluation.md) 复用条件匹配的基线；启动成功或固定动作回放不能替代质量结果。
3. **把评测用在能作决定的地方。** 先用覆盖主要损失及可能代价的实例筛选，再给有希望的候选完整运行和适当独立确认。预留实例或独立运行条件用于检验选择后的表现；重复调试案例只能说明稳定性。针对指定 benchmark 优化是正当目标，跨 benchmark 泛化不是默认附加任务。官方满分需要相应官方证据。
4. **保留最好版本，推进剩余差距。** 按 [执行流程](references/autonomous-research.md) 和 [短记录](references/experiment-card.md) 保存候选去留及下一步。若局部微调连续无效，重新检查目标口径、表示、算法组合、邻域、执行策略、候选覆盖与计算分配；扩大思考范围，不据此宣布全局上限。

试错应逐步缩小选择：先比较少量有区别的方案，看到有效趋势再细化。避免无目的的大网格、每轮全量 A/B 或只挑有利案例；同样不能以节约为由省掉质量评估。短交付写清 **当前最好成绩 → 本轮改变 → 实测得失 → 下一项追分方向**。一个候选失败不等于任务结束，单次服务器退步也不自动证明过拟合。

## 工作边界与交付

在现有授权和资源内自主推进，不每轮请示。未给实验预算时，先了解原生评测成本，以合理范围启动可运行的比较；不要自行发明一个低分停止线。目标验证达到、用户要求停止、明确资源上限耗尽或具体外部条件阻挡必要工作时，保存结果与恢复入口。报告真实缺口；若没有可运行的下一步，列出未排除方向和缺少条件，不能用“到顶了”“收益为负”代替证据，也不捏造进展。

保留使用者工作及最好已验证版本；回退候选只撤销自身改动，不默认 `reset --hard` 或强制推送。发布、外部提交、收费资源和适配器执行遵循会话授权。

## 工具、学习与来源

通用 Markdown 不依赖特定 agent、模型或 API。按需使用 [轨迹工具](references/tooling.md) 检查分歧与预约，用 [质量比较器](references/quality-evaluation.md) 对齐已有实际结果。比较器不运行求解器、不自动晋级。只读当前语言及相关章节；`agents/openai.yaml` 是可选展示信息。执行、真实多 agent 分工和持久运行能力来自宿主，不能假称已运行或会在会话结束后后台工作。

明确要求学习时使用 [提问与提示库](references/question-bank.md)，不把优化接手变成考试。成绩与规则询问属于优化所需的信息收集，不需要启用教学模式。

仅用当前使用者提供或授权的材料。不要主动找回原作者私有工程或历史对话中的源码、精确参数、提交身份、逐例成绩和最终模块组合。公开的是可迁移思想与脱敏观察；合成例须标明，不能当成比赛结果。原实现的环境依赖与代理判据不是通用真理；评分改善和最优性证明是不同结论。
