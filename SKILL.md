---
name: flatland-research-loop
license: CC-BY-NC-4.0
description: "Diagnose Flatland planning and execution failures, locate sources of objective loss, choose changes using real anonymized decision cases, and verify affected behavior with lightweight trace tools and targeted smoke checks. Use for autonomous optimization of the user's own railway planning project or optional interactive learning. 不提供原作者解法或满分配置。"
---

# Flatland Research Loop · 自动研究

**简体中文** | [English](SKILL.en.md)

从使用者自己的代码、评测规则和轨迹中找出问题，形成有依据的修改决定，再用最小必要检查确认。默认自动推进；明确要求学习时才切换交互模式。

## 默认交付

每轮围绕一个当前问题，简短交付：**问题在哪里 → 依据是什么 → 改哪里、为什么 → 用什么小检查确认 → 还有什么未证实。**

观察、解释和已验证修复分开记录。无法定位机制时，下一步是补最小必要观测，而不是凭算法名称或案例结局选择实现。没有值得其成本的下一步时保留当前最好版本并说明停止原因。

## 从诊断到修改

1. **读懂当前系统。** 读取已有目标、接口、代码入口和结果。按 [诊断指南](references/diagnosis.md) 将实际评价、规划、动作执行及修复的相关部分对应起来；只核实影响当前问题的规则，不重建无关全貌。
2. **定位损失与首处分歧。** 选与主目标相关的失败片段，区分规则限制、计划问题、执行分歧、协作等待和计算开销。用源代码和轨迹支持判断；位置不动、等待环或汇总计数本身不证明损失原因。
3. **用案例作决定。** 从 [决策案例](references/reasoning-cases.md) 索引读取相关卡片，依据证据走分支，定位使用者工程中应修改的函数。案例可以被推翻，也可能完全不适用，不按顺序安装其中的方法。
4. **修改并局部验证。** 按 [执行与停止流程](references/autonomous-research.md) 做有依据的改动。优先复用失败小例和已有结果；必要时用 [轨迹工具](references/tooling.md) 检查分歧、等待关系或预约更新，并通过当前项目的适配器真实回放。
5. **保存决定。** 用 [短记录](references/experiment-card.md) 区分当前候选、已确认的修复和性能最好已验证版本。检查通过但性能未测时保持“性能未确认”；只有有价值的未解问题才继续。

不默认跑批量 A/B、参数矩阵、多种子扫描、重复基线或每轮完整评测。冒烟须实际检查受影响行为，不能只看程序启动；它也不证明全局正确、整体提速或评分提高。更广验证需说明会改变哪个决定、为何小检查不足，并遵守现有预算与项目要求。预算是上限，不是消耗目标。

## 工具与阅读方式

通用 Markdown 指南不依赖特定 agent、模型或 API；Python 标准库脚本是可选工具，不是 Flatland 求解器或自动适配器。先检查宿主实际能力，无法执行时交付明确未运行的诊断或检查方案。独立 agent 只用于值得其上下文和协调成本的具体任务，不虚构多方审查。

只读取当前语言和相关参考段落。英文对话使用 [SKILL.en.md](SKILL.en.md) 及其英文参考；无需同时读两套文件。安装发现与调用语法取决于宿主，`agents/openai.yaml` 只是可选展示信息。skill 本身不在会话结束后继续运行。

已有项目材料就自行核实，不反复问使用者。研究对象、目标或授权范围确实缺失且影响工作时，只问关键缺失项；不把尚未回答当作许可。

## 交互学习

明确要求学习时，使用 [提问与提示库](references/question-bank.md)，每次围绕一个关键判断提问，根据回答给渐进提示。已有充分证据就推进；要求直接解释或实现时直接满足，不强制考试。可以用明确标注的合成例练习，不将它写成真实工程结果。

## 来源与结论边界

仅使用当前使用者提供或授权访问的代码、日志和公开材料。不要主动从原作者的私有工程、历史对话或其他任务找回源码、参数、提交身份、逐例成绩及最终模块组合。公开案例不是原解法的复刻说明；本仓库的合成例与工具也不包含它。

达标、可行、评分较好与理论最优是不同主张，分别需要相应证据。保留模块不必然必要，失败方向不必然普遍无效。未知的评分、规则、因果解释和未测性能保持未知，不因来源经历成功就宣称当前用户成功。

本指南不扩展会话授权。只撤销本轮自身改动，保留使用者原有工作；发布、提交、收费资源和执行外部适配器遵循当前任务授权。分享诊断材料前继续保护使用者的数据，不能因为它符合工具格式就公开。
