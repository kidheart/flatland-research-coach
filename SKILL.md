---
name: flatland-research-loop
license: CC-BY-NC-4.0
description: "Improve Flatland solution quality toward the user's measurable objective using algorithmic mechanisms, real anonymized research cases, targeted correctness checks, and actual solver quality evaluation. Preserve the best verified version and change direction when a candidate fails. Agent-agnostic; optional interactive learning. 不提供原作者解法或满分配置。"
---

# Flatland Research Loop · 自动优化

**简体中文** | [English](SKILL.en.md)

默认目标是推进使用者自己的 Flatland 求解质量。把目标差距落实为可改变的规划、搜索或执行决策，实施候选，再用实际求解结果决定是否进步。诊断和冒烟服务于这个目标；明确只修一个错误或学习时，按该范围工作。

## 先确定成功意味着什么

从已有材料核实**主指标与方向、目标值或成功条件、硬约束、最好已验证版本、实际评测入口及资源边界**。保留真实评分、官方本地评价和自定义代理的区别。不了解计分公式时不要自造换算；“100%”是待验证的用户目标，不是算法保证。

默认短交付：**离目标还差什么 → 本轮改变哪个机制及依据 → 实测质量变化和退步 → 候选去留 → 下一项未解决损失。** 无错误的合法计划也可能质量很差，不能因找不到执行分歧就认定没有优化空间。

## 以质量反馈推进

1. **解释当前差距。** 按 [诊断指南](references/diagnosis.md) 对齐评价、代码与运行结果。定位主要损失、已知限制和未解释部分；将单次服务器退步写成“退步、原因待查”，不直接认定过拟合或无法改进。
2. **选能改变决策的算法思想。** 按需读 [算法机制指南](references/algorithm-playbook.md)：状态与时间表示、优先级、联合邻域、依赖协调、修复范围、搜索吞吐和候选覆盖。结合 [真实决策案例](references/reasoning-cases.md) 的失败与有限收益，说明当前方法为什么处理不好这项损失、应改变哪个用户函数、预期哪个实际指标改善。不要按清单叠加算法，也不要停留在阈值微调。
3. **先检查正确性，再测质量。** 针对性冒烟尽早淘汰坏实现。对于优化任务，通过后默认进入 [质量评估](references/quality-evaluation.md)：让候选求解器实际参与代表性实例，检查目标值、主要退步和资源消耗。固定动作回放与程序启动均不能替代它。复用条件一致的基线结果。
4. **让有希望的候选接受验证。** 对筛选有效的候选，用未用于方案选择的相关实例或预先保留的独立运行条件验证；重复筛选案例只能检查稳定性，不能称为未见场景验证。目标涉及官方分数时取得相应官方证据。筛选和验证范围应覆盖改动可能产生的代价，不只选有利案例。不能把局部改善、代理改善或一次筛选直接升级成已达标。
5. **保留最好结果，继续处理差距。** 依据 [执行与停止流程](references/autonomous-research.md) 接纳、拒绝或继续确认候选，用 [短记录](references/experiment-card.md) 保存质量变化。一个候选失败后更新解释、改变机制或选择下一项主要损失；放弃候选不等于结束整个优化任务。

减少无信息量的实验：不默认参数矩阵、种子扫描、全组合消融或每轮全量 A/B。需要的是能选择方案的质量反馈，不能为省时间删掉质量评估。采用足以区分候选的范围，把昂贵的完整评测留给有希望的版本、明确的回归疑问或达标验证；预算是上限，不是消耗目标。

## 未达标时怎样继续或停止

目标已验证达到、使用者要求停止、明确资源耗尽，或下一项必要工作被具体外部条件阻挡时，保存状态并交付实际结果。若认为当前可行方向已穷尽，列出已排除的机制、尚未排除的方向及其所缺条件，说明目标仍未达到；不能用一句“收益为负”“没有值得的下一步”代替这项判断，也不能无限烧预算或捏造进展。按证据报告暂停/受阻，不能报告优化成功。

使用现有授权自主推进，不每轮问答或重复批准。缺失信息确实影响下一步且无法从材料推断时才问。只撤销自身改动并保留原有工作；“保留最好版本”不意味着默认 `reset --hard` 或强制推送，历史重写需有对应授权。

## 工具与按需阅读

通用 Markdown 不依赖特定 agent、模型或 API。Python 标准库工具是可选辅助：用 [轨迹工具](references/tooling.md) 定位分歧、提取片段、检查预约或通过项目适配器回放；用 [质量比较器](references/quality-evaluation.md) 对齐实际评测结果并显示进步、退步及缺失证据。后者不运行求解器，不替代原生评测器，也不自动决定晋级。

只读当前语言与相关章节。英文使用 [SKILL.en.md](SKILL.en.md)；`agents/openai.yaml` 只是可选展示信息。宿主提供文件、执行与真实多 agent 能力；按独立任务价值分工，不虚构审查。缺少执行能力时明确未运行内容，skill 本身不会在会话结束后后台工作。

## 学习与来源边界

明确要求学习时使用 [提问与提示库](references/question-bank.md)，围绕关键判断渐进提问；要求直接讲解或实现时直接满足，不强制考试。合成例必须标明，与真实研究观察分开。

仅使用当前使用者提供或授权访问的材料。不要主动从原作者私有工程或历史对话找回源码、参数、提交身份、逐例成绩和最终模块组合。公开机制是可迁移思想，不是满分配方；原实现里的环境依赖或代理判据也不是通用真理。算法适用性、可行性、实际评分和最优性分别需要证据。发布、外部提交、收费资源和适配器执行遵循会话授权；工具格式不赋予公开用户数据的许可。
